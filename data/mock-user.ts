// Temporary user access data for Free/Premium gating.
// Replace with Firebase auth + subscription state.

import { UserAccess } from '@/types/domain';

export const mockUserAccess: UserAccess = {
  isPremium: false,
};
